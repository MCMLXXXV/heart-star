<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">

    <!-- Standardised web app manifest -->
    <link rel="manifest" href="manifest.json">

    {{> icons }}

    <title>{{ meta.title }}</title>
    <meta name="description" content="{{ meta.description }}">

    <style>
      html { box-sizing: border-box; }
      * { margin: 0; padding: 0; }
      *, *:before, *:after { box-sizing: inherit; }
      html, body { background: #000; overflow: hidden; -ms-touch-action: none; touch-action: none; }
      canvas { touch-action-delay: none; -ms-touch-action: none; touch-action: none; -ms-interpolation-mode: nearest-neighbor; image-rendering: -webkit-optimize-contrast; image-rendering: -moz-crisp-edges; image-rendering: -o-pixelated; image-rendering: pixelated; }
    </style>
  </head>

  <body>
    <!-- build:js game.min.js -->
    <script src="phaser.js"></script>
    <script src="game.js"></script>
    <!-- /build -->
    <script>window.app()</script>
  </body>

</html>
